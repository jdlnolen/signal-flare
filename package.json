{
  "name": "signal-flare",
  "version": "0.1.0",
  "type": "module",
  "description": "Signal Flare bridges Claude Code and Slack — sends rich notifications when Claude needs your input and brings your response back so Claude keeps working.",
  "main": "./dist/server.js",
  "bin": {
    "signal-flare": "./dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "@slack/web-api": "^7.14.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@slack/types": "latest",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
